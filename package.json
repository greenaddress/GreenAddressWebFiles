{
  "name": "greenaddress-webfiles",
  "version": "1.0.0",
  "description": "You can just load the directory in Chrome extentions by enabling the developer mode.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r ./build && mkdir build && python render_templates.py build && cp -r ./static ./build/",
    "sync": "rsync -av ./build/ ../WalletCrx/",
    "watch": "nodemon --watch static --exec 'npm run build && npm run sync'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greenaddress/GreenAddressWebFiles.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/greenaddress/GreenAddressWebFiles/issues"
  },
  "homepage": "https://github.com/greenaddress/GreenAddressWebFiles#readme",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-browserify": "^0.5.1",
    "gulp-clean": "^0.3.2",
    "gulp-run": "^1.6.12",
    "merge-stream": "^1.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  }
}
