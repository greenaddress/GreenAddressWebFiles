language: generic
sudo: required
matrix:
  include:
    - services:
        - docker
      script: docker run -v $PWD:/webfiles greenaddress/webfiles@sha256:2a5cbacce2f754598b40874526acdf3ac506c765fd1aa558a88e1842351888f3 /bin/bash -c "cd /webfiles && ./prepare.sh && virtualenv venv && source venv/bin/activate && pip install -r requirements.txt --require-hashes && python render_templates.py -a .."
